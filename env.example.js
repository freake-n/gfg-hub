/**
 * env.example.js
 * 
 * Instructions:
 * 1. Copy this file and rename it to `env.js` (which is ignored by Git).
 * 2. Fill in your GitHub OAuth App Client ID and Client Secret below.
 * 3. Never commit the real `env.js` file to a public repository!
 */

export const GITHUB_CLIENT_ID     = "YOUR_CLIENT_ID_HERE";
export const GITHUB_CLIENT_SECRET = "YOUR_CLIENT_SECRET_HERE";
